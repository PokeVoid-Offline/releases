From d00840916a1c853ee7f52c8dd70e0a92ff6cca8b Mon Sep 17 00:00:00 2001
From: Jerald Johnson <97370685+Scoooom@users.noreply.github.com>
Date: Thu, 2 Apr 2026 19:04:53 -0500
Subject: [PATCH] Edit appText to indicate offline client

---
 src/ui/handlers/title-ui-handler.ts | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/src/ui/handlers/title-ui-handler.ts b/src/ui/handlers/title-ui-handler.ts
index 8196efa73e1c..20309ed62a51 100644
--- a/src/ui/handlers/title-ui-handler.ts
+++ b/src/ui/handlers/title-ui-handler.ts
@@ -3,7 +3,7 @@ import { loggedInUser } from "#app/account";
 import { FAKE_TITLE_LOGO_CHANCE } from "#app/constants";
 import { timedEventManager } from "#app/global-event-manager";
 import { globalScene } from "#app/global-scene";
-import { isBeta, isDev } from "#constants/app-constants";
+import { isBeta, isDev, isApp } from "#constants/app-constants";
 import { getSplashMessages } from "#data/splash-messages";
 import { PlayerGender } from "#enums/player-gender";
 import type { SpeciesId } from "#enums/species-id";
@@ -194,8 +194,9 @@ export class TitleUiHandler extends OptionSelectUiHandler {
     );
 
     const betaText = isBeta || isDev ? " (Beta)" : "";
-    this.appVersionText.setText("v" + version + betaText);
-
+    const appText = isApp ? " - Unofficial Offline Client (Scooom) Build #BUILD_NUMBER_PLACEHOLDER" : "";
+    this.appVersionText.setText("v" + version + betaText + appText);
+  
     const ui = this.getUi();
 
     if (timedEventManager.isEventActive()) {
